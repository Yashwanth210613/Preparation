class Solution {
    public int jump(int[] nums) {
        int n = nums.length;
        int jump = 0;
        int l = 0;
        int r = 0;
        while(r < n-1){
            int far = 0;
            for(int i=l;i<=r;i++){
                far = Math.max(i+nums[i],far);
            }
            jump++;
            l = r+1;
            r = far;
        }
        return jump;
    }
}
